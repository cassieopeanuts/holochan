{
  "name": "ui",
  "version": "0.1.0",
  "scripts": {
    "start": "vite --clearScreen false --port $UI_PORT",
    "build": "vite build",
    "package": "npm run build && rimraf dist.zip && cd dist && bestzip ../dist.zip *"
  },
  "dependencies": {
    "@holochain-open-dev/context": "^0.0.3",
    "@holochain-open-dev/file-storage": "^0.400.0-dev.1",
    "@holochain/client": "^0.18.0",
    "@msgpack/msgpack": "^2.8.0"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^3.1.2",
    "@tsconfig/svelte": "^3.0.0",
    "bestzip": "^2.2.1",
    "rimraf": "^5.0.10",
    "svelte": "^4.0.0",
    "tslib": "^2.8.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  },
  "type": "module"
}
